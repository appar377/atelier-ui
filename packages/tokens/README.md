# @appar/atelier-tokens

Design Tokens package.

```ts
import { tokens, themes } from '@appar/atelier-tokens'
```

CSS variables are exported separately in the future.

```css
@import "@appar/atelier-tokens/css";
```
